export class SafetyService{constructor({url,serviceKey,fetcher=fetch}){this.url=url;this.serviceKey=serviceKey;this.fetcher=(...args)=>fetcher(...args)}async rpc(name,body){const response=await this.fetcher(`${this.url}/rest/v1/rpc/${name}`,{method:"POST",headers:{apikey:this.serviceKey,authorization:`Bearer ${this.serviceKey}`,"content-type":"application/json"},body:JSON.stringify(body)}),data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.message||`${name}_failed`);return data}like(sessionId,actorRef,targetRef){return this.rpc("record_like",{target_session_id:sessionId,target_actor_ref:actorRef,target_ref:targetRef})}report(sessionId,reporterRef,targetRef,reason){return this.rpc("record_report",{target_session_id:sessionId,target_reporter_ref:reporterRef,target_ref:targetRef,target_reason:reason})}block(blockerUserId,blockedUserId){return this.rpc("create_block",{target_blocker:blockerUserId,target_blocked:blockedUserId})}}
