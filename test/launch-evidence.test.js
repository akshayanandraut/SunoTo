import { describe,it } from "node:test";
import assert from "node:assert/strict";
import { validateLaunchEvidence } from "../scripts/validate-launch-evidence.mjs";

const evidence={revision:"a".repeat(40),staging:{webUrl:"https://staging.random.in",apiUrl:"https://api-staging.random.in/api/v1",smokePassedAt:"2026-08-25T12:00:00Z"},payment:{liveOrderReference:"order_ref",livePaymentReference:"payment_ref",refundReference:"refund_ref",ledgerReconciled:true,reconciledAt:"2026-08-25T12:05:00Z"},advertising:{provider:"reviewed-network",tiersVerifiedAt:"2026-08-25T12:10:00Z",killSwitchVerified:true},load:{reportReference:"LOAD-1",peakVirtualUsers:100,p95Milliseconds:300,p99Milliseconds:700,failureRate:0.005,queuesClean:true,noTranscriptPersistenceVerified:true,completedAt:"2026-08-25T12:20:00Z"},reddit:{postUrl:"https://www.reddit.com/r/india/comments/abc/launch/",monitoringOwner:"operations",postedAt:"2026-08-25T12:30:00Z"}};
describe("Phase 24 evidence gate",()=>{const now=Date.parse("2026-08-25T13:00:00Z");it("accepts evidence bound to the expected release revision",()=>assert.deepEqual(validateLaunchEvidence(evidence,{expectedRevision:"a".repeat(40),now}),{ok:true,errors:[]}));it("rejects the untouched template",()=>{const result=validateLaunchEvidence({}, {now});assert.equal(result.ok,false);assert.ok(result.errors.length>=8);});it("rejects a different release revision and future evidence",()=>{const result=validateLaunchEvidence({...evidence,reddit:{...evidence.reddit,postedAt:"2026-08-26T12:00:00Z"}},{expectedRevision:"b".repeat(40),now});assert.match(result.errors.join("\n"),/expected release commit/);assert.match(result.errors.join("\n"),/future/);});it("rejects load results outside the release budget",()=>{const result=validateLaunchEvidence({...evidence,load:{...evidence.load,p95Milliseconds:600,failureRate:0.02}},{now});assert.equal(result.ok,false);assert.match(result.errors.join("\n"),/threshold/);});});
