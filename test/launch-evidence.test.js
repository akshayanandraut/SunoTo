import { describe,it } from "node:test";
import assert from "node:assert/strict";
import { validateLaunchEvidence } from "../scripts/validate-launch-evidence.mjs";

const evidence={revision:"a".repeat(40),staging:{webUrl:"https://staging.random.in",apiUrl:"https://api-staging.random.in/api/v1",smokePassedAt:"2026-08-25T12:00:00Z"},payment:{liveOrderReference:"order_ref",livePaymentReference:"payment_ref",refundReference:"refund_ref",ledgerReconciled:true},advertising:{provider:"reviewed-network",tiersVerifiedAt:"2026-08-25T12:10:00Z",killSwitchVerified:true},load:{reportReference:"LOAD-1",peakVirtualUsers:100,p95Milliseconds:300,p99Milliseconds:700,failureRate:0.005,queuesClean:true,noTranscriptPersistenceVerified:true},reddit:{postUrl:"https://www.reddit.com/r/india/comments/abc/launch/",monitoringOwner:"operations"}};
describe("Phase 24 evidence gate",()=>{it("accepts structured evidence that meets every threshold",()=>assert.deepEqual(validateLaunchEvidence(evidence),{ok:true,errors:[]}));it("rejects the untouched template",()=>{const result=validateLaunchEvidence({});assert.equal(result.ok,false);assert.ok(result.errors.length>=8);});it("rejects load results outside the release budget",()=>{const result=validateLaunchEvidence({...evidence,load:{...evidence.load,p95Milliseconds:600,failureRate:0.02}});assert.equal(result.ok,false);assert.match(result.errors.join("\n"),/threshold/);});});
